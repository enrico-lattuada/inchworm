# inchworm
A units-and-quantities library, written in Rust

## Use `pre-commit`

1. Install `pre-commit` with
    ```
    pip install pre-commit
    ```
2. Run `pre-commit` on all files
    ```
    pre-commit run --all-files
    ```
